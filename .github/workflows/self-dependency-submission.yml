name: "Self - Dependency Submission"

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Weekly on Sundays at midnight UTC
    - cron: '0 0 * * 0'

permissions:
  contents: write

jobs:
  dependency-submission:
    uses: advanced-security/reusable-workflows/.github/workflows/dependency-submission.yml@main
    secrets: inherit
